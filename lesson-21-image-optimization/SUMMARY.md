# SUMMARY: Урок 21 — Оптимізація зображень

**Чого навчилися:**
- Використовувати компонент `next/image` для локальних та віддалених зображень.
- Працювати з пропсами `priority`, `fill` та класами `object-cover`/`object-contain`.
- Налаштовувати безпечні домени через `remotePatterns` у `next.config.ts`.
- Реалізовувати "Sticky Footer" за допомогою Flexbox (`grow`).

**Результат:** 
Головна сторінка отримала стильний Hero-банер, а хедер — автоматичну аватарку користувача.
